names = ["LogisticRegression", "RidgeClassifier", "SGDClassifier", "RandomForestClassifier",
         "GradientBoostingClassifier", "HistGradientBoostingClassifier", "LinearSVC"]

for name in sorted(names):

    print(name)

names = ["RandomForestRegressor", "GradientBoostingRegressor", "HistGradientBoostingRegressor", "Ridge", "KernelRidge",
         "SGDRegressor", "LinearSVR", "BayesianRidge"]

for name in sorted(names):

    print(name)
